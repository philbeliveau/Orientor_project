Handover Summary - Stability Branch (Current Live Branch)
Branch & Deployment Context:
- I've been working in the stability branch. Previously, I was merging into main and deploying from
there, but to reduce friction, I've now pointed Railway to deploy directly from stability.
- Be aware: both main and stability contain the day's work. If you roll back main, ensure you're not
losing changes only in stability.
CORS Fix & Routing Updates:
- Resolved a CORS issue caused by a wildcard (*) in allowed origins - this is insecure and must
never be used. I've corrected this along with allowed methods and response headers.
- This fix has revealed issues in the onboarding flow:
- A token is issued to the user, but Claude flagged the token format as incorrect and required a
refactor.
- The token issuance route during onboarding currently returns a 404. This seems to stem from
how routes are configured in main_deploy.py.
Routing Observations:
- I've started resolving routing issues by including onboarding routes in main_deploy.py. However,
there are route duplications that need untangling.
- Note: Vercel proxies frontend API calls with an /api prefix, so backend routes must account for that
- I've updated the relevant routes accordingly.
Dependencies & Build Issues:
- The updated requirements.txt includes all necessary packages, but it makes the deployment image
too large, causing build failures.
- You'll need to use a custom image pre-loaded with the heavy ML dependencies, and update the
code to work with that. Railway has guides on doing this - highly recommended for both speed and
stability.
Next Steps:
- Use Claude's code view to inspect my commit history and see what changes I've made.
- Double-check anything generated by Claude - this project is approaching complexity limits where
issues can easily creep in.
- I found the SuperClaude GitHub repo helpful for navigating Claude-generated systems.
Let me know if anything's unclear - good luck and thanks for taking this forward.