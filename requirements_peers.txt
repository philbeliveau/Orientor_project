# Core dependencies
fastapi==0.103.1
sqlalchemy==2.0.20
alembic==1.12.0
pydantic[email]>=2.7.0
uvicorn==0.23.2
python-dotenv==1.0.0
psycopg2-binary==2.9.7

# Vector embeddings
sentence-transformers==2.2.2
pgvector==0.2.0

# Install with:
# pip install -r requirements_peers.txt
#
# Then install pgvector extension in PostgreSQL:
# CREATE EXTENSION IF NOT EXISTS vector;
#
# Then run the migration:
# alembic upgrade head
#
# Finally, generate embeddings:
# python backend/scripts/generate_embeddings.py 